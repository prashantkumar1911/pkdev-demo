# Writing github actions workflow for build java apps, upload artifacts to githhub packages and then push to dockerhub.
---
name: Java CI with Maven
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v4
          
        - name: Set up JDK 21
          uses: actions/setup-java@v4
          with:
            java-version: '21'
            distribution: 'temurin'
        
        - name: Make .mvnw executable
          run: chmod +x ./mvnw

        - name: Build with Maven
          run: ./mvnw clean package

        - name: Upload artifact to GitHub Packages
          uses: actions/upload-artifact@v4
          with:
            name: pkdev-demo-artifact
            path: target/*.jar

        - name: Verify target directory
          run: ls -l target/

    
